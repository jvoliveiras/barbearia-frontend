import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4682B4',
    },
    secondary: {
      main: '#000000',
    },
    background: {
      default: '#F5F5F5',
    },
    text: {
      primary: '#333333',
    },
  },
});

export default theme;
